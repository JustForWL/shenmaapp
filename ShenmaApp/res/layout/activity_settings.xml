<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/settingsmain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.jiaxiaotong.menu.SettingsActivity" >
    
    <Button
        android:id="@+id/chgPwdMainBtn"
        android:layout_width="150dip"
        android:layout_height="40dip"
        android:layout_alignLeft="@+id/takePicBtn"
        android:layout_alignRight="@+id/chsPicBtn"
        android:layout_below="@+id/takePicBtn"
        android:layout_marginBottom="15dip"
        android:layout_marginTop="25dip"
        android:text="修改密码" />

    <ImageView
        android:id="@+id/accountPic"
        android:layout_width="100dip"
        android:layout_height="140dip"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp" />

    <Button
        android:id="@+id/takePicBtn"
        android:layout_width="150dip"
        android:layout_height="50dip"
        android:layout_alignBaseline="@+id/chsPicBtn"
        android:layout_alignBottom="@+id/chsPicBtn"
        android:layout_alignLeft="@+id/pwd_view"
        android:text="拍照" />

    <Button
        android:id="@+id/chsPicBtn"
        android:layout_width="150dip"
        android:layout_height="50dip"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/takePicBtn"
        android:text="选择相片" />

    <View
        android:id="@+id/pwd_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/takePicBtn"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
